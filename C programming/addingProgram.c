/*simple program to add two numbers*/
#include <stdio.h>

int main (void)
{
    int v1,v2,sum;
    v1 = 20;
    v2 = 59;

    sum = v1 + v2;

    printf("The sum of %i and %i is %i\n",v1,v2,sum);
    return 0;
}