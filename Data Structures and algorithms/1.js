//given an array of integers nums, write  function that returns the first element of the array.

let nums = [1,2,3,4,5];

function firstElement(arr){
    return arr[0];
};

console.log(firstElement(nums));